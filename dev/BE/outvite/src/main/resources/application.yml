spring:
  application.name: outvite
  datasource:
    url: jdbc:mysql://localhost:3306/outvite
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create